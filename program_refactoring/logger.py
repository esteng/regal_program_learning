import datetime 
import logging 
import pdb 

# logname = f"new_logs/experiment_{datetime.datetime.now().strftime('%Y-%m-%d_%H_%M')}.log"
# logging.basicConfig(filename=logname,
#                 filemode='a',
#                 format='%(asctime)s,%(msecs)d %(name)s %(levelname)s %(message)s',
#                 datefmt='%H:%M:%S',
#                 level=logging.INFO)

# prog_logger = logging.getLogger(__name__)

